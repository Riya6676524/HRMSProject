
@model List<FirstProject1.Models.student>

@{
    ViewBag.Title = "Students List";
}

<div class="container text-center mt-5">
    <h1>Welcome to the Student Management System</h1>
    <p>Manage Students, Courses, and Enrollments efficiently.</p>

    <div class="mt-4">
        <a href="@Url.Action("Login", "User")" class="btn btn-primary">Login</a>
        <a href="@Url.Action("SignUp", "User")" class="btn btn-success">Sign Up</a>
    </div>
</div>

